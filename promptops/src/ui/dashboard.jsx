/**
 * Starter React component for a dashboard.
 * This is a placeholder; use Next.js or React app to expand.
 */

import React from 'react';

export default function Dashboard() {
  return (
    <div style={{ padding: 24 }}>
      <h1>PromptOps Dashboard (stub)</h1>
      <p>Upload requirement briefs, generate PRDs, and push to tools.</p>
    </div>
  );
}
